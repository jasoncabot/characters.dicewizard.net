version: "2"

sql:
  - engine: "sqlite"
    schema: "migrations"
    queries: "queries.sql"
    gen:
      go:
        package: "store"
        out: "."
        emit_json_tags: true
        json_tags_case_style: camel
        emit_pointers_for_null_types: true
